import {Dimensions} from 'react-native';

export const commonStyle = Object.freeze({
  KEY_COLOR: '#19C084',
  SUB_COLOR: '#ef4f52',
  SCREEN_WIDTH: Dimensions.get('screen').width,
  WHITE: '#fff',
});
